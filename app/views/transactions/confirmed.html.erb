
<div class="container-dois">
    <div id="typed-strings">
        <p>Incrível!_</p>
    </div>
    <span id="typed"></span>
  <br>
  <p>Uau! A partir de agora você vai se divertir pra caralho!</p>
  <div class="card" style="width: 40rem; border: none;">
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><b><%= @transaction.item.category.upcase %></b> <%= @transaction.item.console_model %> - <%= @transaction.item.game_title %></li>
      <li class="list-group-item">De <%= l @transaction.start_date, format: "%d de %B de %Y" %> até <%= l @transaction.end_date, format: "%d de %B de %Y" %></li>
      <li class="list-group-item">R$ <%= @transaction.item.price %> x <%= ((@transaction.end_date - @transaction.start_date)/1.day).round %> dia(s)</li>
      <li class="list-group-item">Total: R$ <%= @transaction.cost %></li>
    </ul>
  </div>
  <br>
    <p>Em caso de dúvidas, entre em contato com <b><%= @transaction.item.user.first_name %></b> <%= @transaction.item.user.last_name %> - <b><%= @transaction.item.user.email %></b></p>
      <%= link_to "Ver todas as transações", transactions_path, class: "btn btn-outline-success mb-2 mr-sm-2" %>
</div>
