<% period = Time.diff(@transaction.end_date, @transaction.start_date, "%d")  %>

<div class="container-dois">
    <div id="typed-strings">
        <p>Revisar_</p>
    </div>
    <span id="typed"></span>
  <br>
  <div class="card" style="width: 40rem; border: none">
    <ul class="list-group list-group-flush">
      <li class="list-group-item"><b><%= @transaction.item.category.upcase %></b> <%= @transaction.item.console_model %> - <%= @transaction.item.game_title %></li>
      <li class="list-group-item">De <%= @transaction.start_date.strftime("%B %d, %Y") %> até <%= @transaction.end_date.strftime("%B %d, %Y") %></li>
      <li class="list-group-item">R$ <%= @transaction.item.price %> x <%= period[:day] %> dia(s)</li>
      <li class="list-group-item">Total: R$ <%= @transaction.cost %></li>
    </ul>
  </div>
  <br>
      <%= link_to "Confirmar", confirmed_path(@transaction), class: "btn btn-outline-success mb-2 mr-sm-2" %>
      <%= link_to "Novo orçamento", new_budget_path(@transaction), class: "btn btn-outline-success mb-2 mr-sm-2", method: :post %>
</div>
