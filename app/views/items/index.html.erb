<% @items.each do |item| %>
  <% if item.game_title == "" %>
    <h2><%= link_to item.console_model, item_path(item) %><h2>
  <% else %>
    <h2><%= link_to item.game_title, item_path(item) %></h2>
  <% end %>
  <% if item.game_title == "" %>
    <h3><%= "" %><h2>
  <% else %>
    <h3>Console: <%= item.console_model %></h3>
  <% end %>
  <p><%= item.category %></p>
  <p>R$<%= item.price %> / dia</p>
  <br>
<% end %>

